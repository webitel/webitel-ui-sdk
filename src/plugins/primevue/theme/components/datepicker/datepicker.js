import { DatePickerScheme } from '@webitel/styleguide/component-schemes';

const datePicker = {
	root: DatePickerScheme.sizes,
	colorScheme: DatePickerScheme.colorScheme,
};

export default datePicker;
