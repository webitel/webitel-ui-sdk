/**
 * TODO(types): every field the tree reads (`itemLabel`, `itemData`,
 * `childrenProp`, `searchedProp`) is a prop-driven runtime key.
 */
// biome-ignore lint/suspicious/noExplicitAny: see TODO above
export type WtTreeNode = any;
