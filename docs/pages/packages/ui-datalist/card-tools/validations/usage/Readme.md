# Card Tools: Validations – Usage

## TLDR;

1. Взяли згенеровану в `@webitel/api-services` `Zod`-схему.
2. Прокинули її у `createCardStore`.
3. З `createCardStore()` у компоненті витягли `Regle`-валідацію.
4. ... ним і користуємось.
