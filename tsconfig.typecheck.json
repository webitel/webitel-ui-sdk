{
	// Root-only CI / local typecheck — excludes packages/* (each package has its own job).
	"extends": "./tsconfig.base.json",
	"include": [
		"src/**/*.ts",
		"docs/pages/**/*.ts",
		"env.d.ts"
	]
}
