# `@webitel/styleguide`

## Build steps

1. `npm ci` - install dependencies
2. `npm version patch --git-tag-version false` - bump version
3. `npm run build` - build
4. `npm publish --access public` - publish library