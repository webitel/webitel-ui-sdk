/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Webitel API
 * OpenAPI spec version: 24.04.0
 */
import * as zod from 'zod';

/**
 * @summary ListVersions returns the version history of an article (newest first).
 */
export const ListVersionsParams = zod.object({
	article_id: zod.string().describe('Owning article id.'),
});

export const ListVersionsQueryParams = zod.object({
	size: zod.int().optional().describe('Page size.'),
	page: zod.int().optional().describe('Page number (1-based).'),
	sort: zod.string().optional().describe('Sorting criteria (e.g. field:asc).'),
	fields: zod
		.array(zod.string())
		.optional()
		.describe('Set of fields to return.'),
});

export const ListVersionsResponse = zod
	.object({
		items: zod
			.array(
				zod
					.object({
						article_id: zod.string().optional().describe('Owning article id.'),
						body_markdown: zod
							.string()
							.optional()
							.describe('Markdown serialization (chunking input).'),
						body_plain: zod
							.string()
							.optional()
							.describe('Plaintext serialization (full-text source).'),
						body_rich_text: zod
							.looseObject({})
							.optional()
							.describe('Editor document for this version.'),
						created_at: zod
							.string()
							.optional()
							.describe('CreatedAt timestamp (epoch ms).'),
						created_by: zod
							.object({
								id: zod.string().optional(),
								name: zod.string().optional(),
							})
							.optional()
							.describe('User who authored the version.'),
						id: zod
							.string()
							.optional()
							.describe('Unique identifier of the version.'),
						notes: zod.string().optional().describe('Optional author notes.'),
						restored_from: zod
							.string()
							.optional()
							.describe(
								'Version this one was restored from; 0 if authored directly.',
							),
						subject: zod.string().optional().describe('Versioned subject.'),
						version_number: zod
							.int()
							.optional()
							.describe('Monotonic version number within the article.'),
					})
					.describe(
						'ArticleVersion is an immutable snapshot of article content.',
					),
			)
			.optional()
			.describe('Versions on this page.'),
		next: zod.boolean().optional().describe('Whether a next page exists.'),
	})
	.describe('ArticleVersionList is a page of versions.');

/**
 * @summary GetVersion returns a single version by number.
 */
export const GetVersionParams = zod.object({
	article_id: zod.string().describe('Owning article id.'),
	version_number: zod.int().describe('Version number.'),
});

export const GetVersionResponse = zod
	.object({
		article_id: zod.string().optional().describe('Owning article id.'),
		body_markdown: zod
			.string()
			.optional()
			.describe('Markdown serialization (chunking input).'),
		body_plain: zod
			.string()
			.optional()
			.describe('Plaintext serialization (full-text source).'),
		body_rich_text: zod
			.looseObject({})
			.optional()
			.describe('Editor document for this version.'),
		created_at: zod
			.string()
			.optional()
			.describe('CreatedAt timestamp (epoch ms).'),
		created_by: zod
			.object({
				id: zod.string().optional(),
				name: zod.string().optional(),
			})
			.optional()
			.describe('User who authored the version.'),
		id: zod.string().optional().describe('Unique identifier of the version.'),
		notes: zod.string().optional().describe('Optional author notes.'),
		restored_from: zod
			.string()
			.optional()
			.describe('Version this one was restored from; 0 if authored directly.'),
		subject: zod.string().optional().describe('Versioned subject.'),
		version_number: zod
			.int()
			.optional()
			.describe('Monotonic version number within the article.'),
	})
	.describe('ArticleVersion is an immutable snapshot of article content.');

/**
 * @summary RestoreVersion creates a new version from a past one.
 */
export const RestoreVersionParams = zod.object({
	article_id: zod.string().describe('Owning article id.'),
	version_number: zod.int().describe('Version number to restore.'),
});

export const RestoreVersionBody = zod
	.object({
		notes: zod
			.string()
			.optional()
			.describe('Optional notes for the new version.'),
	})
	.describe('RestoreVersionRequest selects a version to restore.');

export const RestoreVersionResponse = zod
	.object({
		article_id: zod.string().optional().describe('Owning article id.'),
		body_markdown: zod
			.string()
			.optional()
			.describe('Markdown serialization (chunking input).'),
		body_plain: zod
			.string()
			.optional()
			.describe('Plaintext serialization (full-text source).'),
		body_rich_text: zod
			.looseObject({})
			.optional()
			.describe('Editor document for this version.'),
		created_at: zod
			.string()
			.optional()
			.describe('CreatedAt timestamp (epoch ms).'),
		created_by: zod
			.object({
				id: zod.string().optional(),
				name: zod.string().optional(),
			})
			.optional()
			.describe('User who authored the version.'),
		id: zod.string().optional().describe('Unique identifier of the version.'),
		notes: zod.string().optional().describe('Optional author notes.'),
		restored_from: zod
			.string()
			.optional()
			.describe('Version this one was restored from; 0 if authored directly.'),
		subject: zod.string().optional().describe('Versioned subject.'),
		version_number: zod
			.int()
			.optional()
			.describe('Monotonic version number within the article.'),
	})
	.describe('ArticleVersion is an immutable snapshot of article content.');
