/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Webitel API
 * OpenAPI spec version: 24.04.0
 */
import * as zod from 'zod';

/**
 * (Device) {
 * @summary Register PUSH subscription. App token authentication required
 */
export const CustomerRegisterDeviceBody = zod.object({
	push: zod
		.object({
			APN: zod.string().optional(),
			FCM: zod.string().optional(),
			secret: zod.string().optional(),
			web: zod
				.object({
					endpoint: zod.string().optional(),
					key: zod
						.object({
							auth: zod.string().optional(),
							p256dh: zod.string().optional(),
						})
						.optional(),
				})
				.optional(),
		})
		.optional()
		.describe(
			'PUSH subscription update.\nSet provider type specific registered device token.\nNone to deregister device PUSH subscription.',
		),
});

export const CustomerRegisterDeviceResponse = zod.looseObject({});

/**
 * @summary Invalidate the user's session access token
 */
export const CustomerLogoutResponse = zod
	.object({
		cause: zod
			.object({
				code: zod.int().optional(),
				details: zod
					.array(
						zod.object({
							'@type': zod.string().optional(),
						}),
					)
					.optional(),
				message: zod.string().optional(),
			})
			.optional(),
	})
	.describe(
		'UpdateDisconnect notifies the client about\nan imminent disconnect due to specified reason.',
	);

/**
 * @summary Inspect authentication of the user's session access token
 */
export const CustomerInspectResponse = zod.object({
	access_token: zod
		.string()
		.optional()
		.describe('REQUIRED. The access token issued by the authorization server.'),
	call: zod
		.object({
			auth_id: zod.string().optional(),
			proxy: zod.string().optional(),
			realm: zod.string().optional(),
			secret: zod.string().optional(),
			user_id: zod.string().optional(),
			video: zod.boolean().optional(),
		})
		.optional()
		.describe('VoIP / SIP service credentials.'),
	chat: zod
		.object({
			user: zod
				.object({
					id: zod
						.string()
						.optional()
						.describe(
							'Contact unique **ID**entifier.\nContact **type**-specific string.',
						),
					name: zod.string().optional().describe('Contact display **name**.'),
					type: zod.string().optional().describe('Contact **type** provider.'),
				})
				.optional()
				.describe('User self. The sender. You.'),
		})
		.optional()
		.describe('[I]nstant [M]essaging dialog state.'),
	expires_in: zod
		.int()
		.optional()
		.describe('RECOMMENDED. The lifetime in seconds of the access token.'),
	refresh_token: zod
		.string()
		.optional()
		.describe(
			'OPTIONAL. The refresh token, which can be used to obtain\nnew access tokens using the same authorization grant.',
		),
	scope: zod
		.array(zod.string())
		.optional()
		.describe(
			'OPTIONAL, if identical to the scope requested by the client;\notherwise, REQUIRED. The scope of the access token.',
		),
	state: zod
		.string()
		.optional()
		.describe(
			'REQUIRED if the "state" parameter was present in the client\nauthorization request. The exact value received from the client.',
		),
	token_type: zod
		.string()
		.optional()
		.describe(
			'REQUIRED. The type of the token issued. Value is case insensitive.',
		),
	user: zod
		.object({
			id: zod.string().optional(),
			identity: zod
				.object({
					birthdate: zod
						.string()
						.optional()
						.describe(
							"OPTIONAL. End-User's birthday, represented as an ISO 8601:2004 [ISO8601‑2004] YYYY-MM-DD format.\nThe year MAY be 0000, indicating that it is omitted.\nTo represent only the year, YYYY format is allowed.",
						),
					created_at: zod.string().optional(),
					deleted_at: zod.string().optional(),
					email: zod
						.string()
						.optional()
						.describe(
							"End-User's preferred e-mail address.\nIts value MUST conform to the RFC 5322 [RFC5322] addr-spec syntax.\nThe RP MUST NOT rely upon this value being unique, as discussed in Section 5.7.",
						),
					email_verified: zod
						.boolean()
						.optional()
						.describe(
							"True if the End-User's e-mail address has been verified; otherwise false.",
						),
					family_name: zod
						.string()
						.optional()
						.describe(
							'Surname(s) or last name(s) of the End-User.\nNote that in some cultures, people can have multiple family names or no family name;\nall can be present, with the names being separated by space characters.',
						),
					gender: zod
						.string()
						.optional()
						.describe(
							"OPTIONAL. End-User's gender.\nValues defined by this specification are `female` and `male`.\nOther values MAY be used when neither of the defined values are applicable.",
						),
					given_name: zod
						.string()
						.optional()
						.describe(
							'Given name(s) or first name(s) of the End-User.\nNote that in some cultures, people can have multiple given names;\nall can be present, with the names being separated by space characters.',
						),
					iss: zod
						.string()
						.optional()
						.describe(
							'REQUIRED. Issuer Identifier for the Issuer of the response.\nThe iss value is a case sensitive URL using the https scheme that contains scheme, host,\nand optionally, port number and path components and no query or fragment components.',
						),
					locale: zod
						.string()
						.optional()
						.describe(
							"End-User's locale, represented as a BCP47 [RFC5646] language tag.\nThis is typically an ISO 639-1 Alpha-2 [ISO639‑1] language code in lowercase\nand an ISO 3166-1 Alpha-2 [ISO3166‑1] country code in uppercase,\nseparated by a dash. For example, `en-US` or `uk-UA`.",
						),
					metadata: zod
						.looseObject({})
						.optional()
						.describe("End-User's extra attributes metadata."),
					middle_name: zod
						.string()
						.optional()
						.describe(
							'Middle name(s) of the End-User.\nNote that in some cultures, people can have multiple middle names;\nall can be present, with the names being separated by space characters.\nAlso note that in some cultures, middle names are not used.',
						),
					name: zod
						.string()
						.optional()
						.describe(
							"REQUIRED. End-User's full name in displayable form including all name parts,\npossibly including titles and suffixes, ordered according to the End-User's locale and preferences.",
						),
					phone_number: zod
						.string()
						.optional()
						.describe(
							"End-User's preferred telephone number.\nE.164 is RECOMMENDED as the format of this Claim, for example, +1 (425) 555-1212 or +56 (2) 687 2400.\nIf the phone number contains an extension, it is RECOMMENDED that\nthe extension be represented using the RFC 3966 [RFC3966] extension syntax, for example, +1 (604) 555-1234;ext=5678.",
						),
					phone_number_verified: zod
						.boolean()
						.optional()
						.describe(
							"True if the End-User's phone number has been verified; otherwise false.",
						),
					picture: zod
						.string()
						.optional()
						.describe(
							"OPTIONAL. URL of the End-User's profile picture.\nThis URL MUST refer to an image file\n(for example, a PNG, JPEG, or GIF image file),\nrather than to a Web page containing an image.",
						),
					profile: zod
						.string()
						.optional()
						.describe(
							"OPTIONAL. URL of the End-User's profile page.\nThe contents of this Web page SHOULD be about the End-User.\nNOTE: Issuer SP (IdP) related URL.",
						),
					sources: zod
						.array(
							zod.object({
								iss: zod
									.string()
									.optional()
									.describe(
										'REQUIRED. Issuer Identifier for the Issuer of the response.\nThe iss value is a case sensitive URL using the https scheme that contains scheme, host,\nand optionally, port number and path components and no query or fragment components.',
									),
								sub: zod
									.string()
									.optional()
									.describe(
										'REQUIRED. Subject Identifier.\nA locally unique and never reassigned identifier within the Issuer for the End-User,\nwhich is intended to be consumed by the Client, e.g., 24400320 or AItOawmwtWwcT0k51BayewNvutrJUqsvl6qs7A4.\nIt MUST NOT exceed 255 ASCII characters in length.\nThe sub value is a case sensitive string.',
									),
							}),
						)
						.optional()
						.describe(
							'Additional source(s) associated with given "identity".\nUsed to merge two or more well-known end-user subjects.',
						),
					sub: zod
						.string()
						.optional()
						.describe(
							'REQUIRED. Subject Identifier.\nA locally unique and never reassigned identifier within the Issuer for the End-User,\nwhich is intended to be consumed by the Client, e.g., 24400320 or AItOawmwtWwcT0k51BayewNvutrJUqsvl6qs7A4.\nIt MUST NOT exceed 255 ASCII characters in length.\nThe sub value is a case sensitive string.',
						),
					updated_at: zod
						.string()
						.optional()
						.describe(
							"Time the End-User's information was last updated.\nIts value is a JSON number representing the number of seconds from 1970-01-01T0:0:0Z as measured in UTC until the date/time.",
						),
					zoneinfo: zod
						.string()
						.optional()
						.describe(
							"OPTIONAL. String from zoneinfo [zoneinfo] time zone database representing the End-User's time zone.\nFor example, Europe/Kyiv or America/Los_Angeles.",
						),
				})
				.optional()
				.describe('READONLY. End-User profile info.'),
			sources: zod
				.array(
					zod.object({
						birthdate: zod
							.string()
							.optional()
							.describe(
								"OPTIONAL. End-User's birthday, represented as an ISO 8601:2004 [ISO8601‑2004] YYYY-MM-DD format.\nThe year MAY be 0000, indicating that it is omitted.\nTo represent only the year, YYYY format is allowed.",
							),
						created_at: zod.string().optional(),
						deleted_at: zod.string().optional(),
						email: zod
							.string()
							.optional()
							.describe(
								"End-User's preferred e-mail address.\nIts value MUST conform to the RFC 5322 [RFC5322] addr-spec syntax.\nThe RP MUST NOT rely upon this value being unique, as discussed in Section 5.7.",
							),
						email_verified: zod
							.boolean()
							.optional()
							.describe(
								"True if the End-User's e-mail address has been verified; otherwise false.",
							),
						family_name: zod
							.string()
							.optional()
							.describe(
								'Surname(s) or last name(s) of the End-User.\nNote that in some cultures, people can have multiple family names or no family name;\nall can be present, with the names being separated by space characters.',
							),
						gender: zod
							.string()
							.optional()
							.describe(
								"OPTIONAL. End-User's gender.\nValues defined by this specification are `female` and `male`.\nOther values MAY be used when neither of the defined values are applicable.",
							),
						given_name: zod
							.string()
							.optional()
							.describe(
								'Given name(s) or first name(s) of the End-User.\nNote that in some cultures, people can have multiple given names;\nall can be present, with the names being separated by space characters.',
							),
						iss: zod
							.string()
							.optional()
							.describe(
								'REQUIRED. Issuer Identifier for the Issuer of the response.\nThe iss value is a case sensitive URL using the https scheme that contains scheme, host,\nand optionally, port number and path components and no query or fragment components.',
							),
						locale: zod
							.string()
							.optional()
							.describe(
								"End-User's locale, represented as a BCP47 [RFC5646] language tag.\nThis is typically an ISO 639-1 Alpha-2 [ISO639‑1] language code in lowercase\nand an ISO 3166-1 Alpha-2 [ISO3166‑1] country code in uppercase,\nseparated by a dash. For example, `en-US` or `uk-UA`.",
							),
						metadata: zod
							.looseObject({})
							.optional()
							.describe("End-User's extra attributes metadata."),
						middle_name: zod
							.string()
							.optional()
							.describe(
								'Middle name(s) of the End-User.\nNote that in some cultures, people can have multiple middle names;\nall can be present, with the names being separated by space characters.\nAlso note that in some cultures, middle names are not used.',
							),
						name: zod
							.string()
							.optional()
							.describe(
								"REQUIRED. End-User's full name in displayable form including all name parts,\npossibly including titles and suffixes, ordered according to the End-User's locale and preferences.",
							),
						phone_number: zod
							.string()
							.optional()
							.describe(
								"End-User's preferred telephone number.\nE.164 is RECOMMENDED as the format of this Claim, for example, +1 (425) 555-1212 or +56 (2) 687 2400.\nIf the phone number contains an extension, it is RECOMMENDED that\nthe extension be represented using the RFC 3966 [RFC3966] extension syntax, for example, +1 (604) 555-1234;ext=5678.",
							),
						phone_number_verified: zod
							.boolean()
							.optional()
							.describe(
								"True if the End-User's phone number has been verified; otherwise false.",
							),
						picture: zod
							.string()
							.optional()
							.describe(
								"OPTIONAL. URL of the End-User's profile picture.\nThis URL MUST refer to an image file\n(for example, a PNG, JPEG, or GIF image file),\nrather than to a Web page containing an image.",
							),
						profile: zod
							.string()
							.optional()
							.describe(
								"OPTIONAL. URL of the End-User's profile page.\nThe contents of this Web page SHOULD be about the End-User.\nNOTE: Issuer SP (IdP) related URL.",
							),
						sources: zod
							.array(
								zod.object({
									iss: zod
										.string()
										.optional()
										.describe(
											'REQUIRED. Issuer Identifier for the Issuer of the response.\nThe iss value is a case sensitive URL using the https scheme that contains scheme, host,\nand optionally, port number and path components and no query or fragment components.',
										),
									sub: zod
										.string()
										.optional()
										.describe(
											'REQUIRED. Subject Identifier.\nA locally unique and never reassigned identifier within the Issuer for the End-User,\nwhich is intended to be consumed by the Client, e.g., 24400320 or AItOawmwtWwcT0k51BayewNvutrJUqsvl6qs7A4.\nIt MUST NOT exceed 255 ASCII characters in length.\nThe sub value is a case sensitive string.',
										),
								}),
							)
							.optional()
							.describe(
								'Additional source(s) associated with given "identity".\nUsed to merge two or more well-known end-user subjects.',
							),
						sub: zod
							.string()
							.optional()
							.describe(
								'REQUIRED. Subject Identifier.\nA locally unique and never reassigned identifier within the Issuer for the End-User,\nwhich is intended to be consumed by the Client, e.g., 24400320 or AItOawmwtWwcT0k51BayewNvutrJUqsvl6qs7A4.\nIt MUST NOT exceed 255 ASCII characters in length.\nThe sub value is a case sensitive string.',
							),
						updated_at: zod
							.string()
							.optional()
							.describe(
								"Time the End-User's information was last updated.\nIts value is a JSON number representing the number of seconds from 1970-01-01T0:0:0Z as measured in UTC until the date/time.",
							),
						zoneinfo: zod
							.string()
							.optional()
							.describe(
								"OPTIONAL. String from zoneinfo [zoneinfo] time zone database representing the End-User's time zone.\nFor example, Europe/Kyiv or America/Los_Angeles.",
							),
					}),
				)
				.optional(),
			username: zod
				.string()
				.optional()
				.describe(
					'OPTIONAL.\nShorthand name by which the End-User\nwishes to be referred to at the RP,\nsuch as janedoe or j.doe.',
				),
		})
		.optional()
		.describe('End-User account profile.'),
});

/**
 * @summary Obtain your user's access token to portal service(s)
 */
export const CustomerTokenBody = zod.object({
	app_token: zod
		.string()
		.optional()
		.describe(
			'REQUIRED. Confidential client authorization token.\nMay be transmitted in header: X-Portal-Client.\nKeep it a secret.',
		),
	code: zod
		.string()
		.optional()
		.describe(
			'Authorization code grant.\nREQUIRED. When grant_type is set to "authorization_code".',
		),
	grant_type: zod.string().optional(),
	identity: zod
		.object({
			birthdate: zod
				.string()
				.optional()
				.describe(
					"OPTIONAL. End-User's birthday, represented as an ISO 8601:2004 [ISO8601‑2004] YYYY-MM-DD format.\nThe year MAY be 0000, indicating that it is omitted.\nTo represent only the year, YYYY format is allowed.",
				),
			created_at: zod.string().optional(),
			deleted_at: zod.string().optional(),
			email: zod
				.string()
				.optional()
				.describe(
					"End-User's preferred e-mail address.\nIts value MUST conform to the RFC 5322 [RFC5322] addr-spec syntax.\nThe RP MUST NOT rely upon this value being unique, as discussed in Section 5.7.",
				),
			email_verified: zod
				.boolean()
				.optional()
				.describe(
					"True if the End-User's e-mail address has been verified; otherwise false.",
				),
			family_name: zod
				.string()
				.optional()
				.describe(
					'Surname(s) or last name(s) of the End-User.\nNote that in some cultures, people can have multiple family names or no family name;\nall can be present, with the names being separated by space characters.',
				),
			gender: zod
				.string()
				.optional()
				.describe(
					"OPTIONAL. End-User's gender.\nValues defined by this specification are `female` and `male`.\nOther values MAY be used when neither of the defined values are applicable.",
				),
			given_name: zod
				.string()
				.optional()
				.describe(
					'Given name(s) or first name(s) of the End-User.\nNote that in some cultures, people can have multiple given names;\nall can be present, with the names being separated by space characters.',
				),
			iss: zod
				.string()
				.optional()
				.describe(
					'REQUIRED. Issuer Identifier for the Issuer of the response.\nThe iss value is a case sensitive URL using the https scheme that contains scheme, host,\nand optionally, port number and path components and no query or fragment components.',
				),
			locale: zod
				.string()
				.optional()
				.describe(
					"End-User's locale, represented as a BCP47 [RFC5646] language tag.\nThis is typically an ISO 639-1 Alpha-2 [ISO639‑1] language code in lowercase\nand an ISO 3166-1 Alpha-2 [ISO3166‑1] country code in uppercase,\nseparated by a dash. For example, `en-US` or `uk-UA`.",
				),
			metadata: zod
				.looseObject({})
				.optional()
				.describe("End-User's extra attributes metadata."),
			middle_name: zod
				.string()
				.optional()
				.describe(
					'Middle name(s) of the End-User.\nNote that in some cultures, people can have multiple middle names;\nall can be present, with the names being separated by space characters.\nAlso note that in some cultures, middle names are not used.',
				),
			name: zod
				.string()
				.optional()
				.describe(
					"REQUIRED. End-User's full name in displayable form including all name parts,\npossibly including titles and suffixes, ordered according to the End-User's locale and preferences.",
				),
			phone_number: zod
				.string()
				.optional()
				.describe(
					"End-User's preferred telephone number.\nE.164 is RECOMMENDED as the format of this Claim, for example, +1 (425) 555-1212 or +56 (2) 687 2400.\nIf the phone number contains an extension, it is RECOMMENDED that\nthe extension be represented using the RFC 3966 [RFC3966] extension syntax, for example, +1 (604) 555-1234;ext=5678.",
				),
			phone_number_verified: zod
				.boolean()
				.optional()
				.describe(
					"True if the End-User's phone number has been verified; otherwise false.",
				),
			picture: zod
				.string()
				.optional()
				.describe(
					"OPTIONAL. URL of the End-User's profile picture.\nThis URL MUST refer to an image file\n(for example, a PNG, JPEG, or GIF image file),\nrather than to a Web page containing an image.",
				),
			profile: zod
				.string()
				.optional()
				.describe(
					"OPTIONAL. URL of the End-User's profile page.\nThe contents of this Web page SHOULD be about the End-User.\nNOTE: Issuer SP (IdP) related URL.",
				),
			sources: zod
				.array(
					zod.object({
						iss: zod
							.string()
							.optional()
							.describe(
								'REQUIRED. Issuer Identifier for the Issuer of the response.\nThe iss value is a case sensitive URL using the https scheme that contains scheme, host,\nand optionally, port number and path components and no query or fragment components.',
							),
						sub: zod
							.string()
							.optional()
							.describe(
								'REQUIRED. Subject Identifier.\nA locally unique and never reassigned identifier within the Issuer for the End-User,\nwhich is intended to be consumed by the Client, e.g., 24400320 or AItOawmwtWwcT0k51BayewNvutrJUqsvl6qs7A4.\nIt MUST NOT exceed 255 ASCII characters in length.\nThe sub value is a case sensitive string.',
							),
					}),
				)
				.optional()
				.describe(
					'Additional source(s) associated with given "identity".\nUsed to merge two or more well-known end-user subjects.',
				),
			sub: zod
				.string()
				.optional()
				.describe(
					'REQUIRED. Subject Identifier.\nA locally unique and never reassigned identifier within the Issuer for the End-User,\nwhich is intended to be consumed by the Client, e.g., 24400320 or AItOawmwtWwcT0k51BayewNvutrJUqsvl6qs7A4.\nIt MUST NOT exceed 255 ASCII characters in length.\nThe sub value is a case sensitive string.',
				),
			updated_at: zod
				.string()
				.optional()
				.describe(
					"Time the End-User's information was last updated.\nIts value is a JSON number representing the number of seconds from 1970-01-01T0:0:0Z as measured in UTC until the date/time.",
				),
			zoneinfo: zod
				.string()
				.optional()
				.describe(
					"OPTIONAL. String from zoneinfo [zoneinfo] time zone database representing the End-User's time zone.\nFor example, Europe/Kyiv or America/Los_Angeles.",
				),
		})
		.optional()
		.describe(
			'Identity of the end-User account association.\nREQUIRED. When grant_type is set to "identity".',
		),
	push: zod
		.object({
			APN: zod.string().optional(),
			FCM: zod.string().optional(),
			secret: zod.string().optional(),
			web: zod
				.object({
					endpoint: zod.string().optional(),
					key: zod
						.object({
							auth: zod.string().optional(),
							p256dh: zod.string().optional(),
						})
						.optional(),
				})
				.optional(),
		})
		.optional()
		.describe('OPTIONAL. Sign up client device for PUSH notifications.'),
	refresh_token: zod
		.string()
		.optional()
		.describe(
			'Refresh token string to obtain NEW access token.\nREQUIRED. When grant_type is set to "refresh_token".',
		),
	response_type: zod.array(zod.string()).optional(),
	scope: zod.array(zod.string()).optional(),
	state: zod.string().optional(),
});

export const CustomerTokenResponse = zod.object({
	access_token: zod
		.string()
		.optional()
		.describe('REQUIRED. The access token issued by the authorization server.'),
	call: zod
		.object({
			auth_id: zod.string().optional(),
			proxy: zod.string().optional(),
			realm: zod.string().optional(),
			secret: zod.string().optional(),
			user_id: zod.string().optional(),
			video: zod.boolean().optional(),
		})
		.optional()
		.describe('VoIP / SIP service credentials.'),
	chat: zod
		.object({
			user: zod
				.object({
					id: zod
						.string()
						.optional()
						.describe(
							'Contact unique **ID**entifier.\nContact **type**-specific string.',
						),
					name: zod.string().optional().describe('Contact display **name**.'),
					type: zod.string().optional().describe('Contact **type** provider.'),
				})
				.optional()
				.describe('User self. The sender. You.'),
		})
		.optional()
		.describe('[I]nstant [M]essaging dialog state.'),
	expires_in: zod
		.int()
		.optional()
		.describe('RECOMMENDED. The lifetime in seconds of the access token.'),
	refresh_token: zod
		.string()
		.optional()
		.describe(
			'OPTIONAL. The refresh token, which can be used to obtain\nnew access tokens using the same authorization grant.',
		),
	scope: zod
		.array(zod.string())
		.optional()
		.describe(
			'OPTIONAL, if identical to the scope requested by the client;\notherwise, REQUIRED. The scope of the access token.',
		),
	state: zod
		.string()
		.optional()
		.describe(
			'REQUIRED if the "state" parameter was present in the client\nauthorization request. The exact value received from the client.',
		),
	token_type: zod
		.string()
		.optional()
		.describe(
			'REQUIRED. The type of the token issued. Value is case insensitive.',
		),
	user: zod
		.object({
			id: zod.string().optional(),
			identity: zod
				.object({
					birthdate: zod
						.string()
						.optional()
						.describe(
							"OPTIONAL. End-User's birthday, represented as an ISO 8601:2004 [ISO8601‑2004] YYYY-MM-DD format.\nThe year MAY be 0000, indicating that it is omitted.\nTo represent only the year, YYYY format is allowed.",
						),
					created_at: zod.string().optional(),
					deleted_at: zod.string().optional(),
					email: zod
						.string()
						.optional()
						.describe(
							"End-User's preferred e-mail address.\nIts value MUST conform to the RFC 5322 [RFC5322] addr-spec syntax.\nThe RP MUST NOT rely upon this value being unique, as discussed in Section 5.7.",
						),
					email_verified: zod
						.boolean()
						.optional()
						.describe(
							"True if the End-User's e-mail address has been verified; otherwise false.",
						),
					family_name: zod
						.string()
						.optional()
						.describe(
							'Surname(s) or last name(s) of the End-User.\nNote that in some cultures, people can have multiple family names or no family name;\nall can be present, with the names being separated by space characters.',
						),
					gender: zod
						.string()
						.optional()
						.describe(
							"OPTIONAL. End-User's gender.\nValues defined by this specification are `female` and `male`.\nOther values MAY be used when neither of the defined values are applicable.",
						),
					given_name: zod
						.string()
						.optional()
						.describe(
							'Given name(s) or first name(s) of the End-User.\nNote that in some cultures, people can have multiple given names;\nall can be present, with the names being separated by space characters.',
						),
					iss: zod
						.string()
						.optional()
						.describe(
							'REQUIRED. Issuer Identifier for the Issuer of the response.\nThe iss value is a case sensitive URL using the https scheme that contains scheme, host,\nand optionally, port number and path components and no query or fragment components.',
						),
					locale: zod
						.string()
						.optional()
						.describe(
							"End-User's locale, represented as a BCP47 [RFC5646] language tag.\nThis is typically an ISO 639-1 Alpha-2 [ISO639‑1] language code in lowercase\nand an ISO 3166-1 Alpha-2 [ISO3166‑1] country code in uppercase,\nseparated by a dash. For example, `en-US` or `uk-UA`.",
						),
					metadata: zod
						.looseObject({})
						.optional()
						.describe("End-User's extra attributes metadata."),
					middle_name: zod
						.string()
						.optional()
						.describe(
							'Middle name(s) of the End-User.\nNote that in some cultures, people can have multiple middle names;\nall can be present, with the names being separated by space characters.\nAlso note that in some cultures, middle names are not used.',
						),
					name: zod
						.string()
						.optional()
						.describe(
							"REQUIRED. End-User's full name in displayable form including all name parts,\npossibly including titles and suffixes, ordered according to the End-User's locale and preferences.",
						),
					phone_number: zod
						.string()
						.optional()
						.describe(
							"End-User's preferred telephone number.\nE.164 is RECOMMENDED as the format of this Claim, for example, +1 (425) 555-1212 or +56 (2) 687 2400.\nIf the phone number contains an extension, it is RECOMMENDED that\nthe extension be represented using the RFC 3966 [RFC3966] extension syntax, for example, +1 (604) 555-1234;ext=5678.",
						),
					phone_number_verified: zod
						.boolean()
						.optional()
						.describe(
							"True if the End-User's phone number has been verified; otherwise false.",
						),
					picture: zod
						.string()
						.optional()
						.describe(
							"OPTIONAL. URL of the End-User's profile picture.\nThis URL MUST refer to an image file\n(for example, a PNG, JPEG, or GIF image file),\nrather than to a Web page containing an image.",
						),
					profile: zod
						.string()
						.optional()
						.describe(
							"OPTIONAL. URL of the End-User's profile page.\nThe contents of this Web page SHOULD be about the End-User.\nNOTE: Issuer SP (IdP) related URL.",
						),
					sources: zod
						.array(
							zod.object({
								iss: zod
									.string()
									.optional()
									.describe(
										'REQUIRED. Issuer Identifier for the Issuer of the response.\nThe iss value is a case sensitive URL using the https scheme that contains scheme, host,\nand optionally, port number and path components and no query or fragment components.',
									),
								sub: zod
									.string()
									.optional()
									.describe(
										'REQUIRED. Subject Identifier.\nA locally unique and never reassigned identifier within the Issuer for the End-User,\nwhich is intended to be consumed by the Client, e.g., 24400320 or AItOawmwtWwcT0k51BayewNvutrJUqsvl6qs7A4.\nIt MUST NOT exceed 255 ASCII characters in length.\nThe sub value is a case sensitive string.',
									),
							}),
						)
						.optional()
						.describe(
							'Additional source(s) associated with given "identity".\nUsed to merge two or more well-known end-user subjects.',
						),
					sub: zod
						.string()
						.optional()
						.describe(
							'REQUIRED. Subject Identifier.\nA locally unique and never reassigned identifier within the Issuer for the End-User,\nwhich is intended to be consumed by the Client, e.g., 24400320 or AItOawmwtWwcT0k51BayewNvutrJUqsvl6qs7A4.\nIt MUST NOT exceed 255 ASCII characters in length.\nThe sub value is a case sensitive string.',
						),
					updated_at: zod
						.string()
						.optional()
						.describe(
							"Time the End-User's information was last updated.\nIts value is a JSON number representing the number of seconds from 1970-01-01T0:0:0Z as measured in UTC until the date/time.",
						),
					zoneinfo: zod
						.string()
						.optional()
						.describe(
							"OPTIONAL. String from zoneinfo [zoneinfo] time zone database representing the End-User's time zone.\nFor example, Europe/Kyiv or America/Los_Angeles.",
						),
				})
				.optional()
				.describe('READONLY. End-User profile info.'),
			sources: zod
				.array(
					zod.object({
						birthdate: zod
							.string()
							.optional()
							.describe(
								"OPTIONAL. End-User's birthday, represented as an ISO 8601:2004 [ISO8601‑2004] YYYY-MM-DD format.\nThe year MAY be 0000, indicating that it is omitted.\nTo represent only the year, YYYY format is allowed.",
							),
						created_at: zod.string().optional(),
						deleted_at: zod.string().optional(),
						email: zod
							.string()
							.optional()
							.describe(
								"End-User's preferred e-mail address.\nIts value MUST conform to the RFC 5322 [RFC5322] addr-spec syntax.\nThe RP MUST NOT rely upon this value being unique, as discussed in Section 5.7.",
							),
						email_verified: zod
							.boolean()
							.optional()
							.describe(
								"True if the End-User's e-mail address has been verified; otherwise false.",
							),
						family_name: zod
							.string()
							.optional()
							.describe(
								'Surname(s) or last name(s) of the End-User.\nNote that in some cultures, people can have multiple family names or no family name;\nall can be present, with the names being separated by space characters.',
							),
						gender: zod
							.string()
							.optional()
							.describe(
								"OPTIONAL. End-User's gender.\nValues defined by this specification are `female` and `male`.\nOther values MAY be used when neither of the defined values are applicable.",
							),
						given_name: zod
							.string()
							.optional()
							.describe(
								'Given name(s) or first name(s) of the End-User.\nNote that in some cultures, people can have multiple given names;\nall can be present, with the names being separated by space characters.',
							),
						iss: zod
							.string()
							.optional()
							.describe(
								'REQUIRED. Issuer Identifier for the Issuer of the response.\nThe iss value is a case sensitive URL using the https scheme that contains scheme, host,\nand optionally, port number and path components and no query or fragment components.',
							),
						locale: zod
							.string()
							.optional()
							.describe(
								"End-User's locale, represented as a BCP47 [RFC5646] language tag.\nThis is typically an ISO 639-1 Alpha-2 [ISO639‑1] language code in lowercase\nand an ISO 3166-1 Alpha-2 [ISO3166‑1] country code in uppercase,\nseparated by a dash. For example, `en-US` or `uk-UA`.",
							),
						metadata: zod
							.looseObject({})
							.optional()
							.describe("End-User's extra attributes metadata."),
						middle_name: zod
							.string()
							.optional()
							.describe(
								'Middle name(s) of the End-User.\nNote that in some cultures, people can have multiple middle names;\nall can be present, with the names being separated by space characters.\nAlso note that in some cultures, middle names are not used.',
							),
						name: zod
							.string()
							.optional()
							.describe(
								"REQUIRED. End-User's full name in displayable form including all name parts,\npossibly including titles and suffixes, ordered according to the End-User's locale and preferences.",
							),
						phone_number: zod
							.string()
							.optional()
							.describe(
								"End-User's preferred telephone number.\nE.164 is RECOMMENDED as the format of this Claim, for example, +1 (425) 555-1212 or +56 (2) 687 2400.\nIf the phone number contains an extension, it is RECOMMENDED that\nthe extension be represented using the RFC 3966 [RFC3966] extension syntax, for example, +1 (604) 555-1234;ext=5678.",
							),
						phone_number_verified: zod
							.boolean()
							.optional()
							.describe(
								"True if the End-User's phone number has been verified; otherwise false.",
							),
						picture: zod
							.string()
							.optional()
							.describe(
								"OPTIONAL. URL of the End-User's profile picture.\nThis URL MUST refer to an image file\n(for example, a PNG, JPEG, or GIF image file),\nrather than to a Web page containing an image.",
							),
						profile: zod
							.string()
							.optional()
							.describe(
								"OPTIONAL. URL of the End-User's profile page.\nThe contents of this Web page SHOULD be about the End-User.\nNOTE: Issuer SP (IdP) related URL.",
							),
						sources: zod
							.array(
								zod.object({
									iss: zod
										.string()
										.optional()
										.describe(
											'REQUIRED. Issuer Identifier for the Issuer of the response.\nThe iss value is a case sensitive URL using the https scheme that contains scheme, host,\nand optionally, port number and path components and no query or fragment components.',
										),
									sub: zod
										.string()
										.optional()
										.describe(
											'REQUIRED. Subject Identifier.\nA locally unique and never reassigned identifier within the Issuer for the End-User,\nwhich is intended to be consumed by the Client, e.g., 24400320 or AItOawmwtWwcT0k51BayewNvutrJUqsvl6qs7A4.\nIt MUST NOT exceed 255 ASCII characters in length.\nThe sub value is a case sensitive string.',
										),
								}),
							)
							.optional()
							.describe(
								'Additional source(s) associated with given "identity".\nUsed to merge two or more well-known end-user subjects.',
							),
						sub: zod
							.string()
							.optional()
							.describe(
								'REQUIRED. Subject Identifier.\nA locally unique and never reassigned identifier within the Issuer for the End-User,\nwhich is intended to be consumed by the Client, e.g., 24400320 or AItOawmwtWwcT0k51BayewNvutrJUqsvl6qs7A4.\nIt MUST NOT exceed 255 ASCII characters in length.\nThe sub value is a case sensitive string.',
							),
						updated_at: zod
							.string()
							.optional()
							.describe(
								"Time the End-User's information was last updated.\nIts value is a JSON number representing the number of seconds from 1970-01-01T0:0:0Z as measured in UTC until the date/time.",
							),
						zoneinfo: zod
							.string()
							.optional()
							.describe(
								"OPTIONAL. String from zoneinfo [zoneinfo] time zone database representing the End-User's time zone.\nFor example, Europe/Kyiv or America/Los_Angeles.",
							),
					}),
				)
				.optional(),
			username: zod
				.string()
				.optional()
				.describe(
					'OPTIONAL.\nShorthand name by which the End-User\nwishes to be referred to at the RP,\nsuch as janedoe or j.doe.',
				),
		})
		.optional()
		.describe('End-User account profile.'),
});
