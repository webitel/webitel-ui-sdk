/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Webitel API
 * OpenAPI spec version: 24.04.0
 */
import * as zod from 'zod';

/**
 * @summary / CreateCustomGate registers an external system as a messaging gateway.
 */
export const CustomServiceCreateCustomGateBody = zod
	.object({
		allowed_ips: zod.array(zod.string()).optional(),
		app_secret: zod.string().optional(),
		callback_url: zod.string().optional(),
		enabled: zod.boolean().optional(),
		name: zod.string().optional(),
		peer: zod
			.object({
				iss: zod.string().optional(),
				sub: zod.string().optional(),
			})
			.optional(),
		request_timeout_ms: zod.int().optional(),
		retry_attempts: zod.int().optional(),
	})
	.describe(
		'/ ProviderCreateCustomGateRequest registers an arbitrary external communication\n/ system as a chat channel. Only configuration is required on the Webitel side:\n/ the external system posts signed updates to the generated webhook_url and\n/ receives operator replies on callback_url.',
	);

export const customServiceCreateCustomGateResponseItemStatusDefault = `PROVIDER_STATUS_UNSPECIFIED`;

export const CustomServiceCreateCustomGateResponse = zod
	.object({
		item: zod
			.object({
				allowed_ips: zod
					.array(zod.string())
					.optional()
					.describe(
						'Optional CIDR allowlist; an empty list accepts any source address.',
					),
				app_secret: zod
					.string()
					.optional()
					.describe(
						'Shared secret used to sign and verify X-Webitel-Sign in both directions; handled as sensitive.',
					),
				callback_url: zod
					.string()
					.optional()
					.describe(
						'Customer-side webhook Webitel posts operator replies and events to.',
					),
				created_at: zod.string().optional(),
				enabled: zod.boolean().optional(),
				id: zod.string().optional(),
				name: zod.string().optional(),
				request_timeout_ms: zod
					.int()
					.optional()
					.describe(
						'Timeout of a single outbound callback attempt, in milliseconds.',
					),
				retry_attempts: zod
					.int()
					.optional()
					.describe(
						'How many times an outbound callback is retried before the message is marked failed.',
					),
				status: zod
					.enum([
						'PROVIDER_STATUS_UNSPECIFIED',
						'PROVIDER_STATUS_ACTIVE',
						'PROVIDER_STATUS_INACTIVE',
						'PROVIDER_STATUS_ERROR',
					])
					.default(customServiceCreateCustomGateResponseItemStatusDefault)
					.describe(
						'/ ProviderStatus represents the operational state of a provider gateway.\n\n - PROVIDER_STATUS_ACTIVE: Provider is healthy and processing messages\n - PROVIDER_STATUS_INACTIVE: Manually disabled by user\n - PROVIDER_STATUS_ERROR: Connection or authentication failed',
					),
				updated_at: zod.string().optional(),
				webhook_url: zod
					.string()
					.optional()
					.describe(
						'Webitel-side webhook the external system posts updates to.',
					),
			})
			.optional(),
	})
	.describe(
		'/ ProviderCreateCustomGateResponse returns the newly activated custom provider.',
	);

/**
 * @summary / DeleteCustomGate removes the custom gateway integration.
 */
export const CustomServiceDeleteCustomGateParams = zod.object({
	id: zod.string(),
});

export const customServiceDeleteCustomGateResponseItemStatusDefault = `PROVIDER_STATUS_UNSPECIFIED`;

export const CustomServiceDeleteCustomGateResponse = zod.object({
	item: zod
		.object({
			allowed_ips: zod
				.array(zod.string())
				.optional()
				.describe(
					'Optional CIDR allowlist; an empty list accepts any source address.',
				),
			app_secret: zod
				.string()
				.optional()
				.describe(
					'Shared secret used to sign and verify X-Webitel-Sign in both directions; handled as sensitive.',
				),
			callback_url: zod
				.string()
				.optional()
				.describe(
					'Customer-side webhook Webitel posts operator replies and events to.',
				),
			created_at: zod.string().optional(),
			enabled: zod.boolean().optional(),
			id: zod.string().optional(),
			name: zod.string().optional(),
			request_timeout_ms: zod
				.int()
				.optional()
				.describe(
					'Timeout of a single outbound callback attempt, in milliseconds.',
				),
			retry_attempts: zod
				.int()
				.optional()
				.describe(
					'How many times an outbound callback is retried before the message is marked failed.',
				),
			status: zod
				.enum([
					'PROVIDER_STATUS_UNSPECIFIED',
					'PROVIDER_STATUS_ACTIVE',
					'PROVIDER_STATUS_INACTIVE',
					'PROVIDER_STATUS_ERROR',
				])
				.default(customServiceDeleteCustomGateResponseItemStatusDefault)
				.describe(
					'/ ProviderStatus represents the operational state of a provider gateway.\n\n - PROVIDER_STATUS_ACTIVE: Provider is healthy and processing messages\n - PROVIDER_STATUS_INACTIVE: Manually disabled by user\n - PROVIDER_STATUS_ERROR: Connection or authentication failed',
				),
			updated_at: zod.string().optional(),
			webhook_url: zod
				.string()
				.optional()
				.describe('Webitel-side webhook the external system posts updates to.'),
		})
		.optional(),
});

/**
 * @summary / GetCustomGate retrieves configuration and status for a custom gateway.
 */
export const CustomServiceGetCustomGateParams = zod.object({
	id: zod.string(),
});

export const customServiceGetCustomGateResponseItemStatusDefault = `PROVIDER_STATUS_UNSPECIFIED`;

export const CustomServiceGetCustomGateResponse = zod.object({
	item: zod
		.object({
			allowed_ips: zod
				.array(zod.string())
				.optional()
				.describe(
					'Optional CIDR allowlist; an empty list accepts any source address.',
				),
			app_secret: zod
				.string()
				.optional()
				.describe(
					'Shared secret used to sign and verify X-Webitel-Sign in both directions; handled as sensitive.',
				),
			callback_url: zod
				.string()
				.optional()
				.describe(
					'Customer-side webhook Webitel posts operator replies and events to.',
				),
			created_at: zod.string().optional(),
			enabled: zod.boolean().optional(),
			id: zod.string().optional(),
			name: zod.string().optional(),
			request_timeout_ms: zod
				.int()
				.optional()
				.describe(
					'Timeout of a single outbound callback attempt, in milliseconds.',
				),
			retry_attempts: zod
				.int()
				.optional()
				.describe(
					'How many times an outbound callback is retried before the message is marked failed.',
				),
			status: zod
				.enum([
					'PROVIDER_STATUS_UNSPECIFIED',
					'PROVIDER_STATUS_ACTIVE',
					'PROVIDER_STATUS_INACTIVE',
					'PROVIDER_STATUS_ERROR',
				])
				.default(customServiceGetCustomGateResponseItemStatusDefault)
				.describe(
					'/ ProviderStatus represents the operational state of a provider gateway.\n\n - PROVIDER_STATUS_ACTIVE: Provider is healthy and processing messages\n - PROVIDER_STATUS_INACTIVE: Manually disabled by user\n - PROVIDER_STATUS_ERROR: Connection or authentication failed',
				),
			updated_at: zod.string().optional(),
			webhook_url: zod
				.string()
				.optional()
				.describe('Webitel-side webhook the external system posts updates to.'),
		})
		.optional(),
});

/**
 * @summary / UpdateCustomGate updates custom gateway settings.
 */
export const CustomServiceUpdateCustomGateParams = zod.object({
	id: zod.string(),
});

export const CustomServiceUpdateCustomGateBody = zod
	.object({
		allowed_ips: zod.array(zod.string()).optional(),
		app_secret: zod.string().optional(),
		callback_url: zod.string().optional(),
		enabled: zod.boolean().optional(),
		name: zod.string().optional(),
		peer: zod
			.object({
				iss: zod.string().optional(),
				sub: zod.string().optional(),
			})
			.optional(),
		request_timeout_ms: zod.int().optional(),
		retry_attempts: zod.int().optional(),
	})
	.describe(
		'/ ProviderUpdateCustomGateRequest updates custom gateway settings. A non-empty\n/ app_secret rotates the signing secret and breaks the integration until the\n/ external system is updated too.',
	);

export const customServiceUpdateCustomGateResponseItemStatusDefault = `PROVIDER_STATUS_UNSPECIFIED`;

export const CustomServiceUpdateCustomGateResponse = zod
	.object({
		item: zod
			.object({
				allowed_ips: zod
					.array(zod.string())
					.optional()
					.describe(
						'Optional CIDR allowlist; an empty list accepts any source address.',
					),
				app_secret: zod
					.string()
					.optional()
					.describe(
						'Shared secret used to sign and verify X-Webitel-Sign in both directions; handled as sensitive.',
					),
				callback_url: zod
					.string()
					.optional()
					.describe(
						'Customer-side webhook Webitel posts operator replies and events to.',
					),
				created_at: zod.string().optional(),
				enabled: zod.boolean().optional(),
				id: zod.string().optional(),
				name: zod.string().optional(),
				request_timeout_ms: zod
					.int()
					.optional()
					.describe(
						'Timeout of a single outbound callback attempt, in milliseconds.',
					),
				retry_attempts: zod
					.int()
					.optional()
					.describe(
						'How many times an outbound callback is retried before the message is marked failed.',
					),
				status: zod
					.enum([
						'PROVIDER_STATUS_UNSPECIFIED',
						'PROVIDER_STATUS_ACTIVE',
						'PROVIDER_STATUS_INACTIVE',
						'PROVIDER_STATUS_ERROR',
					])
					.default(customServiceUpdateCustomGateResponseItemStatusDefault)
					.describe(
						'/ ProviderStatus represents the operational state of a provider gateway.\n\n - PROVIDER_STATUS_ACTIVE: Provider is healthy and processing messages\n - PROVIDER_STATUS_INACTIVE: Manually disabled by user\n - PROVIDER_STATUS_ERROR: Connection or authentication failed',
					),
				updated_at: zod.string().optional(),
				webhook_url: zod
					.string()
					.optional()
					.describe(
						'Webitel-side webhook the external system posts updates to.',
					),
			})
			.optional(),
	})
	.describe(
		'/ ProviderUpdateCustomGateResponse returns the updated custom provider.',
	);
