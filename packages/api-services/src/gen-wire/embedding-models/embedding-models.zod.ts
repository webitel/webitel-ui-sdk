/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * Webitel API
 * OpenAPI spec version: 24.04.0
 */
import * as zod from 'zod';

/**
 * @summary ListModels returns models available to the caller's domain (and globals).
 */
export const ListModelsQueryParams = zod.object({
	size: zod.int().optional().describe('Page size.'),
	page: zod.int().optional().describe('Page number (1-based).'),
	q: zod.string().optional().describe('Optional name search.'),
	type: zod
		.string()
		.optional()
		.describe('Optional type filter ("embedding" or "reranker").'),
	sort: zod.string().optional().describe('Sorting criteria (e.g. field:asc).'),
	fields: zod
		.array(zod.string())
		.optional()
		.describe('Set of fields to return.'),
});

export const ListModelsResponse = zod
	.object({
		items: zod
			.array(
				zod
					.object({
						created_at: zod
							.string()
							.optional()
							.describe('CreatedAt timestamp (epoch ms).'),
						created_by: zod
							.object({
								id: zod.string().optional(),
								name: zod.string().optional(),
							})
							.optional()
							.describe('User who created the model.'),
						dimensions: zod
							.int()
							.optional()
							.describe('Embedding dimension; 0 for rerankers.'),
						domain_id: zod
							.string()
							.optional()
							.describe(
								'Owning domain id; 0 for a global (Webitel-seeded) model.',
							),
						endpoint: zod
							.string()
							.optional()
							.describe('Self-hosted / Azure / BYOM endpoint url.'),
						id: zod
							.string()
							.optional()
							.describe('Unique identifier of the model.'),
						is_self_hosted: zod
							.boolean()
							.optional()
							.describe('Whether the model runs self-hosted.'),
						model_ref: zod
							.string()
							.optional()
							.describe('Provider model name or HuggingFace id.'),
						name: zod
							.string()
							.optional()
							.describe('Human-readable name shown in the admin UI.'),
						provider: zod
							.string()
							.optional()
							.describe(
								'Provider key (gemini, openai, cohere, azure, bge-m3, e5, bge-reranker, byom).',
							),
						type: zod
							.string()
							.optional()
							.describe('Model type: "embedding" or "reranker".'),
						validated_at: zod
							.string()
							.optional()
							.describe(
								'Timestamp of the last successful validation (epoch ms); 0 if never validated.',
							),
					})
					.describe(
						'EmbeddingModel is a registry entry read model (credential omitted).',
					),
			)
			.optional()
			.describe('Models on this page.'),
		next: zod.boolean().optional().describe('Whether a next page exists.'),
	})
	.describe('EmbeddingModelList is a page of models.');

/**
 * @summary CreateModel registers a new model.
 */
export const CreateModelBody = zod
	.object({
		api_key: zod
			.string()
			.optional()
			.describe(
				'Write-only provider credential; encrypted at rest, never read back.',
			),
		dimensions: zod
			.int()
			.optional()
			.describe('Embedding dimension; omit for rerankers.'),
		endpoint: zod
			.string()
			.optional()
			.describe('Self-hosted / Azure / BYOM endpoint url.'),
		is_self_hosted: zod
			.boolean()
			.optional()
			.describe('Whether the model runs self-hosted.'),
		model_ref: zod
			.string()
			.optional()
			.describe('Provider model name or HuggingFace id.'),
		name: zod.string().optional().describe('Human-readable name.'),
		provider: zod.string().optional().describe('Provider key.'),
		type: zod
			.string()
			.optional()
			.describe('Model type: "embedding" or "reranker".'),
	})
	.describe('InputEmbeddingModel carries the writable fields of a model.');

export const CreateModelResponse = zod
	.object({
		created_at: zod
			.string()
			.optional()
			.describe('CreatedAt timestamp (epoch ms).'),
		created_by: zod
			.object({
				id: zod.string().optional(),
				name: zod.string().optional(),
			})
			.optional()
			.describe('User who created the model.'),
		dimensions: zod
			.int()
			.optional()
			.describe('Embedding dimension; 0 for rerankers.'),
		domain_id: zod
			.string()
			.optional()
			.describe('Owning domain id; 0 for a global (Webitel-seeded) model.'),
		endpoint: zod
			.string()
			.optional()
			.describe('Self-hosted / Azure / BYOM endpoint url.'),
		id: zod.string().optional().describe('Unique identifier of the model.'),
		is_self_hosted: zod
			.boolean()
			.optional()
			.describe('Whether the model runs self-hosted.'),
		model_ref: zod
			.string()
			.optional()
			.describe('Provider model name or HuggingFace id.'),
		name: zod
			.string()
			.optional()
			.describe('Human-readable name shown in the admin UI.'),
		provider: zod
			.string()
			.optional()
			.describe(
				'Provider key (gemini, openai, cohere, azure, bge-m3, e5, bge-reranker, byom).',
			),
		type: zod
			.string()
			.optional()
			.describe('Model type: "embedding" or "reranker".'),
		validated_at: zod
			.string()
			.optional()
			.describe(
				'Timestamp of the last successful validation (epoch ms); 0 if never validated.',
			),
	})
	.describe(
		'EmbeddingModel is a registry entry read model (credential omitted).',
	);

/**
 * @summary DeleteModel removes a model registration.
 */
export const DeleteModelParams = zod.object({
	id: zod.string().describe('Model id.'),
});

export const DeleteModelResponse = zod
	.object({
		created_at: zod
			.string()
			.optional()
			.describe('CreatedAt timestamp (epoch ms).'),
		created_by: zod
			.object({
				id: zod.string().optional(),
				name: zod.string().optional(),
			})
			.optional()
			.describe('User who created the model.'),
		dimensions: zod
			.int()
			.optional()
			.describe('Embedding dimension; 0 for rerankers.'),
		domain_id: zod
			.string()
			.optional()
			.describe('Owning domain id; 0 for a global (Webitel-seeded) model.'),
		endpoint: zod
			.string()
			.optional()
			.describe('Self-hosted / Azure / BYOM endpoint url.'),
		id: zod.string().optional().describe('Unique identifier of the model.'),
		is_self_hosted: zod
			.boolean()
			.optional()
			.describe('Whether the model runs self-hosted.'),
		model_ref: zod
			.string()
			.optional()
			.describe('Provider model name or HuggingFace id.'),
		name: zod
			.string()
			.optional()
			.describe('Human-readable name shown in the admin UI.'),
		provider: zod
			.string()
			.optional()
			.describe(
				'Provider key (gemini, openai, cohere, azure, bge-m3, e5, bge-reranker, byom).',
			),
		type: zod
			.string()
			.optional()
			.describe('Model type: "embedding" or "reranker".'),
		validated_at: zod
			.string()
			.optional()
			.describe(
				'Timestamp of the last successful validation (epoch ms); 0 if never validated.',
			),
	})
	.describe(
		'EmbeddingModel is a registry entry read model (credential omitted).',
	);

/**
 * @summary LocateModel returns a single model by id.
 */
export const LocateModelParams = zod.object({
	id: zod.string().describe('Model id.'),
});

export const LocateModelResponse = zod
	.object({
		created_at: zod
			.string()
			.optional()
			.describe('CreatedAt timestamp (epoch ms).'),
		created_by: zod
			.object({
				id: zod.string().optional(),
				name: zod.string().optional(),
			})
			.optional()
			.describe('User who created the model.'),
		dimensions: zod
			.int()
			.optional()
			.describe('Embedding dimension; 0 for rerankers.'),
		domain_id: zod
			.string()
			.optional()
			.describe('Owning domain id; 0 for a global (Webitel-seeded) model.'),
		endpoint: zod
			.string()
			.optional()
			.describe('Self-hosted / Azure / BYOM endpoint url.'),
		id: zod.string().optional().describe('Unique identifier of the model.'),
		is_self_hosted: zod
			.boolean()
			.optional()
			.describe('Whether the model runs self-hosted.'),
		model_ref: zod
			.string()
			.optional()
			.describe('Provider model name or HuggingFace id.'),
		name: zod
			.string()
			.optional()
			.describe('Human-readable name shown in the admin UI.'),
		provider: zod
			.string()
			.optional()
			.describe(
				'Provider key (gemini, openai, cohere, azure, bge-m3, e5, bge-reranker, byom).',
			),
		type: zod
			.string()
			.optional()
			.describe('Model type: "embedding" or "reranker".'),
		validated_at: zod
			.string()
			.optional()
			.describe(
				'Timestamp of the last successful validation (epoch ms); 0 if never validated.',
			),
	})
	.describe(
		'EmbeddingModel is a registry entry read model (credential omitted).',
	);

/**
 * @summary UpdateModel updates a model registration.
 */
export const UpdateModel2Params = zod.object({
	id: zod.string().describe('Model id.'),
});

export const UpdateModel2Body = zod
	.object({
		api_key: zod
			.string()
			.optional()
			.describe(
				'Write-only provider credential; encrypted at rest, never read back.',
			),
		dimensions: zod
			.int()
			.optional()
			.describe('Embedding dimension; omit for rerankers.'),
		endpoint: zod
			.string()
			.optional()
			.describe('Self-hosted / Azure / BYOM endpoint url.'),
		is_self_hosted: zod
			.boolean()
			.optional()
			.describe('Whether the model runs self-hosted.'),
		model_ref: zod
			.string()
			.optional()
			.describe('Provider model name or HuggingFace id.'),
		name: zod.string().optional().describe('Human-readable name.'),
		provider: zod.string().optional().describe('Provider key.'),
		type: zod
			.string()
			.optional()
			.describe('Model type: "embedding" or "reranker".'),
	})
	.describe('InputEmbeddingModel carries the writable fields of a model.');

export const UpdateModel2Response = zod
	.object({
		created_at: zod
			.string()
			.optional()
			.describe('CreatedAt timestamp (epoch ms).'),
		created_by: zod
			.object({
				id: zod.string().optional(),
				name: zod.string().optional(),
			})
			.optional()
			.describe('User who created the model.'),
		dimensions: zod
			.int()
			.optional()
			.describe('Embedding dimension; 0 for rerankers.'),
		domain_id: zod
			.string()
			.optional()
			.describe('Owning domain id; 0 for a global (Webitel-seeded) model.'),
		endpoint: zod
			.string()
			.optional()
			.describe('Self-hosted / Azure / BYOM endpoint url.'),
		id: zod.string().optional().describe('Unique identifier of the model.'),
		is_self_hosted: zod
			.boolean()
			.optional()
			.describe('Whether the model runs self-hosted.'),
		model_ref: zod
			.string()
			.optional()
			.describe('Provider model name or HuggingFace id.'),
		name: zod
			.string()
			.optional()
			.describe('Human-readable name shown in the admin UI.'),
		provider: zod
			.string()
			.optional()
			.describe(
				'Provider key (gemini, openai, cohere, azure, bge-m3, e5, bge-reranker, byom).',
			),
		type: zod
			.string()
			.optional()
			.describe('Model type: "embedding" or "reranker".'),
		validated_at: zod
			.string()
			.optional()
			.describe(
				'Timestamp of the last successful validation (epoch ms); 0 if never validated.',
			),
	})
	.describe(
		'EmbeddingModel is a registry entry read model (credential omitted).',
	);

/**
 * @summary UpdateModel updates a model registration.
 */
export const UpdateModelParams = zod.object({
	id: zod.string().describe('Model id.'),
});

export const UpdateModelBody = zod
	.object({
		api_key: zod
			.string()
			.optional()
			.describe(
				'Write-only provider credential; encrypted at rest, never read back.',
			),
		dimensions: zod
			.int()
			.optional()
			.describe('Embedding dimension; omit for rerankers.'),
		endpoint: zod
			.string()
			.optional()
			.describe('Self-hosted / Azure / BYOM endpoint url.'),
		is_self_hosted: zod
			.boolean()
			.optional()
			.describe('Whether the model runs self-hosted.'),
		model_ref: zod
			.string()
			.optional()
			.describe('Provider model name or HuggingFace id.'),
		name: zod.string().optional().describe('Human-readable name.'),
		provider: zod.string().optional().describe('Provider key.'),
		type: zod
			.string()
			.optional()
			.describe('Model type: "embedding" or "reranker".'),
	})
	.describe('InputEmbeddingModel carries the writable fields of a model.');

export const UpdateModelResponse = zod
	.object({
		created_at: zod
			.string()
			.optional()
			.describe('CreatedAt timestamp (epoch ms).'),
		created_by: zod
			.object({
				id: zod.string().optional(),
				name: zod.string().optional(),
			})
			.optional()
			.describe('User who created the model.'),
		dimensions: zod
			.int()
			.optional()
			.describe('Embedding dimension; 0 for rerankers.'),
		domain_id: zod
			.string()
			.optional()
			.describe('Owning domain id; 0 for a global (Webitel-seeded) model.'),
		endpoint: zod
			.string()
			.optional()
			.describe('Self-hosted / Azure / BYOM endpoint url.'),
		id: zod.string().optional().describe('Unique identifier of the model.'),
		is_self_hosted: zod
			.boolean()
			.optional()
			.describe('Whether the model runs self-hosted.'),
		model_ref: zod
			.string()
			.optional()
			.describe('Provider model name or HuggingFace id.'),
		name: zod
			.string()
			.optional()
			.describe('Human-readable name shown in the admin UI.'),
		provider: zod
			.string()
			.optional()
			.describe(
				'Provider key (gemini, openai, cohere, azure, bge-m3, e5, bge-reranker, byom).',
			),
		type: zod
			.string()
			.optional()
			.describe('Model type: "embedding" or "reranker".'),
		validated_at: zod
			.string()
			.optional()
			.describe(
				'Timestamp of the last successful validation (epoch ms); 0 if never validated.',
			),
	})
	.describe(
		'EmbeddingModel is a registry entry read model (credential omitted).',
	);

/**
 * @summary ValidateModel runs a test embed/rerank call and stamps validated_at on success.
 */
export const ValidateModelParams = zod.object({
	id: zod.string().describe('Model id.'),
});

export const ValidateModelResponse = zod
	.object({
		created_at: zod
			.string()
			.optional()
			.describe('CreatedAt timestamp (epoch ms).'),
		created_by: zod
			.object({
				id: zod.string().optional(),
				name: zod.string().optional(),
			})
			.optional()
			.describe('User who created the model.'),
		dimensions: zod
			.int()
			.optional()
			.describe('Embedding dimension; 0 for rerankers.'),
		domain_id: zod
			.string()
			.optional()
			.describe('Owning domain id; 0 for a global (Webitel-seeded) model.'),
		endpoint: zod
			.string()
			.optional()
			.describe('Self-hosted / Azure / BYOM endpoint url.'),
		id: zod.string().optional().describe('Unique identifier of the model.'),
		is_self_hosted: zod
			.boolean()
			.optional()
			.describe('Whether the model runs self-hosted.'),
		model_ref: zod
			.string()
			.optional()
			.describe('Provider model name or HuggingFace id.'),
		name: zod
			.string()
			.optional()
			.describe('Human-readable name shown in the admin UI.'),
		provider: zod
			.string()
			.optional()
			.describe(
				'Provider key (gemini, openai, cohere, azure, bge-m3, e5, bge-reranker, byom).',
			),
		type: zod
			.string()
			.optional()
			.describe('Model type: "embedding" or "reranker".'),
		validated_at: zod
			.string()
			.optional()
			.describe(
				'Timestamp of the last successful validation (epoch ms); 0 if never validated.',
			),
	})
	.describe(
		'EmbeddingModel is a registry entry read model (credential omitted).',
	);
